{
  "name": "pcs-chair-colorizer",
  "version": "0.1.0",
  "scripts": {
    "build": "cd view && npm run build",
    "package": "zip -r extension.zip assets/* view/dist/* manifest.json *.js"
  },
  "author": "MAKinteract",
  "license": "MIT",
  "description": "Chrome Extension for colorizing PCS Chair",
  "dependencies": {
    "@types/chrome": "^0.0.296"
  }
}
